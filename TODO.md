# 남은 작업

완료된 항목은 반복하지 않고, 현재 실제로 남은 작업만 기록합니다.

1. **P0 — 공개 옥산 비경의 실제 플레이 흐름과 전투 화면**
   - `4be14c6` 무치트 7분 런은 승천·결과·재도전까지 통과했고 `f2b9254` 결과 정보 위계도 focused PASS로 닫았습니다. 완료된 결과·악귀 수사·일반 늑대 변형을 다시 만들지 않습니다.
   - 같은 런에서 420초에 35층까지 올라 대형 선택 모달이 매우 자주 전투를 끊었고, 구간 캡처 다수가 모달에 걸렸습니다. XP 요구량·선택 횟수·실제 정지 시간을 먼저 계측해 7분 압축 성장의 손맛은 보존하면서 과도한 중단만 줄입니다.
   - 6분 41초 실제 보스 프레임은 전조·보스 체력·주인공은 읽히지만 후반 군집의 색·실루엣 반복과 지면 평면감은 여전히 상용 후보 기준 미달입니다. 현재 `jade` 로스터와 표현 분배를 먼저 계측하고, 이미 닫힌 변형을 재작성하지 않은 채 가장 큰 화면 결함 하나씩 수정합니다.
   - 수정마다 1920×1080·2560×1600 실제 전투 프레임과 새 무치트 7분 회귀로 판정합니다.

2. **P0 — 권리 증거와 법적 게이트**
   - runtime asset 78개 각각의 법적 권리 상태와 증거를 확보·검토합니다.
   - 0/78 상태가 해소되기 전까지 rights gate는 `BLOCKED`로 유지합니다.

3. **P1 — 잠긴 염화 비경의 `ashRaven` 종 의미**
   - `ashRaven`은 현재 새·비행 영체가 아니라 `yorang` 늑대 아틀라스를 재사용합니다.
   - 현재 release stage는 `jade` 하나이므로 첫 런 P0로 과장하지 않습니다. `ember` 공개 범위 승격 전에 등장 구간·점유를 검증하고 표현만 교체합니다.

4. **P1 — Game2D 번들 경고**
   - 564.82 kB Game2D chunk의 bundle warning을 분할·축소하거나, 남겨야 한다면 근거와 영향 범위를 문서화합니다.
   - 이 경고가 사라져도 release approval이나 A-grade를 자동 부여하지 않습니다.
